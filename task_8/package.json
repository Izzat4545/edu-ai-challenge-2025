{
  "name": "schema-validator",
  "version": "1.0.0",
  "description": "A robust validation library for JavaScript",
  "main": "schema.js",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
